# ROS2 编译产物
build/
install/
log/

# Python 缓存
__pycache__/
*.pyc

# 地图文件（体积大，单独备份）
*.pgm
*.yaml.bak

# 系统文件
.DS_Store


# src/gz_ros2_control/
src/articubot_one/

# Tooling (optional)
.codex
# 注意是 .claude/* 不是 .claude/ —— git 无法重新包含被排除目录下的内容，
# 只有忽略「目录内的条目」才能用 ! 放行其中一个子目录。
.claude/*
!.claude/skills/

# Editor (optional)
.vscode/

# stm32 层：源码/驱动/ioc 全部跟踪（clone 即可编译，CubeMX 改动可 diff review）
# 只排除编译产物与 IDE 本地状态
firmware/my_robot_stm32/Debug/
firmware/my_robot_stm32/.metadata/
firmware/my_robot_stm32/.settings/

# 硬件雷达驱动包
src/sllidar_ros2/
