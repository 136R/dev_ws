> **结果**：待复盘 —— 可观测的三条里两条通过、一条未达标（见【验收】）；
> 回复风格与 skill 触发率属规则类改动，只能等复盘触发点。（2026-08-15）

# 协作约定 v2：回复风格 + 注释规范 + 文档去参数硬编码

## 【目标】

补上 2026-08-04 那轮没覆盖的三件事：**话怎么说**（回复风格）、**注释怎么写**、
**文档为什么不该写参数值**。核心是让文档与注释停止成为"会过时的数值副本"。

## 【事实】

- `.gitignore:25-26` 是 `.claude/*` + `!.claude/skills/` —— skills **已在版本库内**，
  `spec/SKILL.md` 已跟踪。2026-08-04 卡里"skill 不进版本库"的记述现已不成立
- `.claude/settings.json` 已被 git 跟踪，`outputStyle` 字段能进版本库
- 改动前常驻上下文 = CLAUDE.md 111 行 + 约定 83 行 = 194 行
- 条号被 8 张历史 spec 卡与 `spec/SKILL.md` 交叉引用（第 1/4/5/6/7/8/14/15/16 条）
- 注释语言分布：`my_bot_task` 中文 239/英文 42 行，`my_bot_hw` 英文 141/中文 91，
  固件自研代码英文约 180 行（已排除 Seb Madgwick 的 Fusion 库 62 行）
- `话题与频率.md` 第 55-57 行已有前例记录：2026-08-13 之前那里写死的 `0.885`
  是错的（沿用了作废的固件宏、又没算 multiplier）—— **本轮规范要防的就是这个**

### 联网调研（2026-08-15）

- 官方定位表：**output style 用于「每一轮的默认回复格式」**，修改 system prompt 并在会话中
  反复触发遵守提醒；**CLAUDE.md 是在 system prompt 之后追加一条 user message**
- 官方 Default 风格是「少于 4 行、无 preamble/postamble、改完文件直接停」——
  比本轮要的 B 风格**更短**
- 官方点名的失败模式：「over-specified CLAUDE.md —— 太长时 Claude 忽略掉一半规则」
- Claude Code 内置指令含 `IMPORTANT: DO NOT ADD ANY COMMENTS unless asked`，
  2026 年转为「默认不写，只在 WHY 非显然时写：隐藏约束 / 微妙不变量 / 特定 bug 的 workaround /
  反直觉行为」
- 官方 CLAUDE.md ❌Exclude 列表含「Information that changes frequently」
  「Detailed API documentation (link to docs instead)」—— 直接背书文档去数值
- 社区反向观点（Clean Code for AI Agents）：不要删 AI 自己写的注释，那是它给下次编辑留的上下文

## 【假设】

- 假设：output style 比写进约定.md 更可能被遵守
  ｜ 验法：见【验收】的回复风格判据，只能观察下一轮起的实际输出
  ｜ 处置：本轮核心赌注，未验证

- 假设：`code-style` skill 的 description 能在改代码/改文档时可靠触发
  ｜ 验法：下次动 `src/` 或 `docs/` 时看它有没有被自动加载
  ｜ 处置：写完当轮已观察到 skill 进入技能列表，但**"进列表"≠"会触发"**，仍待验

- 假设：B 风格在纯问答场景会膨胀
  ｜ 验法：三场景对比后已确认成立
  ｜ 处置：**已验证并处置** —— 加了「L0 纯事实问答不套结构」退化规则

## 【方案】（已执行）

| 文件 | 动作 |
| --- | --- |
| `.claude/output-styles/conclusion-first.md` | 新建。退化规则 + B 式结构 + 禁止项 + 四种展开触发 |
| `.claude/settings.json` | 加 `"outputStyle": "conclusion-first"` |
| `.gitignore` | 加 `!.claude/output-styles/` |
| `docs/代码与文档规范.md` | 新建。注释规范（默认不写、四种情况、只写指针）+ 文档参数规范 |
| `.claude/skills/code-style/SKILL.md` | 新建薄触发层，三条硬规则 + 指向上面那份 |
| `docs/与我协作的约定.md` | 第 3–7 条压缩（25→11 行），顶部加分工说明 |
| `CLAUDE.md` | 架构图去数值，文档入口加规范指针 |
| `src/my_bot_hw/docs/话题与频率.md` | 频率表、超时表、启动顺序表全部去数值改指针 |
| `src/my_bot_hw/docs/README.md` | 不变量表去数值，列头改「去哪看」 |
| `src/my_bot{,_nav,_slam,_task}/docs/README.md` | 同上 |

关键设计三条：

1. **回复风格用 output style 而非 CLAUDE.md** —— 机制强一档，且不占 200 行预算
2. **条号只增不改** —— 压缩正文字数而不重排编号，保住 8 张历史卡的交叉引用
3. **指针用 `文件` + `键名`，禁止行号** —— 行号漂了 AI 会直接信错行，比没指针更糟

## 【不做】

- **不翻译存量英文注释**（约 274 行 ROS 侧 + 180 行固件）—— 模型读英文无障碍，
  纯翻译收益≈0。规则只约束新写与改动碰到的部分
- 不加 CI 校验脚本（用户否决：新增机制、要长期维护）
- 不加 hook（2026-08-04 用户说先不急）
- 不迁移到 AGENTS.md（本项目只用 Claude Code）
- **不动 `docs/APP/`** —— 按批准的 plan 排除。⚠️ 但那里有 8 处行号指针
  （`map_manager.cpp:362` 等，指向上游 app 仓库）未处理，上游一更新必漂，
  需要时单独一轮
- 不重排约定的条号

## 【验收】

**可观测三条，当场跑过：**

| # | 判据 | 结果 |
| --- | --- | --- |
| 1 | 常驻上下文 ≤ 180 行 | ❌ **188 行**（CLAUDE.md 113 + 约定 75）。未达标 |
| 2 | 各包 README 不变量表无裸数值 | ✅ 0 命中 |
| 3 | 文档无行号指针（排除 spec / APP） | ✅ 仅剩规范文件里那条**反例示范**，合规 |

第 1 条未达标的原因：原计划靠重排条号省 19 行，但条号被 8 张历史卡引用，
改为只压正文、保留编号，实省 14 行；CLAUDE.md 又加了 2 行指针。
194 − 14 + 2 + 6（新增的表格行与说明）= 188。
**不硬凑到 180** —— 继续压会伤到用户逐条定过的第 9–13 条内容。

**回复风格判据（下一轮对话起即可判）：**

- 纯事实问答一行答完，不出现加粗结论行和要点列表
- 产生改动时首行是加粗结论，不是背景铺垫
- 每条要点能指到一个文件/键名/命令
- 无 `##` 小标题（除非确实超 40 行）、无预告、无自我总结

⚠️ **output style 要 `/clear` 或开新会话才生效** —— 本轮写完时尚未重载，
所以本轮之后的回复才是有效样本。

**复盘触发点（不可观测部分）：**

1. **下次动 `src/` 代码或 `docs/` 文档时** —— 看 `code-style` skill 有没有被自动加载。
   没加载 = description 触发条件写得不准，**改 description，不是把规范塞回 CLAUDE.md**
2. **下次说「你回复太长了 / 说重点」时** —— 先查退化规则（L0 不套结构）是不是没触发，
   再查「四种情况才展开」够不够硬
3. **一并补做 2026-08-04 欠的复盘** —— 那张卡要求「累计 3 次 L2 后检查 `docs/spec/` 的卡
   是否都标了结果」，现有 9 张卡、从没做过。多数没标 = 第 8 条形同虚设，
   该简化协议而不是继续加码

## 【异议】

有三条，都**不影响本轮方案对错**，按第 5 条不做成选择题 —— 我仍按用户说的做，
记在这里供复盘时优先怀疑。

1. **三项里只有回复风格有强机制兜底。** output style 进 system prompt 且有会话内提醒；
   skill 靠 description 匹配触发，可靠性中等；`docs/代码与文档规范.md` 完全靠 skill 拉起来。
   注释规范若两周后没生效，第一嫌疑是 skill 的 description，不是规范正文写得不够细。

2. **`话题与频率.md` 的性质变了。** 那份文档原本的存在目的就是集中展示频率数值，
   去数值后变成一张「频率参数在哪」的索引表。这符合规范，但如果用户实际是拿它当
   "快速查频率"用，改造后会不好用。**这一条请在下次真的去查频率时确认。**

3. **本轮在 2026-08-04 那个未验证的赌注上继续加码。** 那轮的最大风险点是
   「规则总量与官方警告冲突」，至今未复盘。本轮虽把常驻从 194 压到 188，
   但同时新增了一份 output style（约 20 行进 system prompt）和一个 skill。
   总规则量是涨的，只是换了载体。
